import Login from 'components/Login';
import Section from 'components/Section';

const LoginPage = () => {
  return (
    <Section variant="login" title="Log In">
      <Login />
    </Section>
  );
};

export default LoginPage;
